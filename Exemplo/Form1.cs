namespace Exemplo
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
            string msg = "Bem-vindo ao repositório do GIT";
            MessageBox.Show(msg);
        }
    }
}
